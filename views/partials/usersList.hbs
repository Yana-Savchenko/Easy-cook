  <table class="table table-bordered">
    <thead class="thead-light">
      <tr>
        <th scope="col" data-name="firstName">First name <span class="fas fa-sort-alpha-{{this.direction}} firstName"></span></th>
        <th scope="col" data-name="lastName">Last name <span class="fas fa-sort-alpha-{{this.direction}} lastName"></span></th>
        <th scope="col" data-name="email">Email <span class="fas fa-sort-alpha-{{this.direction}} email"></span></th>
        <th scope="col" data-name="age">Age <span class="fas fa-sort-numeric-{{this.direction}} age"></span></th>
      </tr>
    </thead>
    {{#each users}}
    <tr>
      <td>{{this.firstName}}</td>
      <td>{{this.lastName}}</td>
      <td>{{this.email}}</td>
      <td>{{this.age}}</td>
    </tr>
    {{/each}}
  </table>
  <nav>
    <ul class="pagination">
      <li class="page-item disabled">
        <a class="page-link" href="#" aria-label="Previous">
          <span aria-hidden="true">&laquo;</span>
          <span class="sr-only">Previous</span>
        </a>
      </li>
      {{#each pages}}
      <li class="page-item  {{#if this.isActive}}active{{/if}}" data-page={{this.content}}><a class="page-link" href="">{{this.content}}</a></li>
      {{/each}}
      <li class="page-item disabled">
        <a class="page-link" href="#" aria-label="Next">
          <span aria-hidden="true">&raquo;</span>
          <span class="sr-only">Next</span>
        </a>
      </li>
    </ul>
  </nav>